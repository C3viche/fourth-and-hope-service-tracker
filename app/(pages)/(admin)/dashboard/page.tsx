import styles from './page.module.scss';

const Dashboard = () => {
    return (
        <main className={styles.page}>
            <h1>Dashboard</h1>
        </main>
    );
}

export default Dashboard;